---
docs_version: 9
title: MCP Server
layout: docs-adoc
menu: cloud
applies_to: cloud
---

Every service can present itself to an AI agent over the **Model Context Protocol**. You choose which collections, aggregations, change streams and GraphQL applications the agent may use, and describe each one so it knows what the thing is for. Nothing is exposed until you publish it.

*Navigation path:* Service → MCP Server

NOTE: MCP is in **beta**. The endpoint and the published catalogue are stable enough to build against; expect the page itself to keep gaining features.

== Your First Agent, in Ten Minutes

Four steps, from a service with a collection in it to Claude answering a question about your data. Nothing here needs code.

. **Publish a collection.** On the **MCP Server** page, **Add collection**, pick one, and write what it holds — a sentence an agent can read to decide whether this is the data it wants. Say `orders`: "Customer orders, one document per order, with status and total."
. **Give the agent a credential.** An agent must not sign in as you. On the **API Keys** page, turn keys on and add a permission: your users' role may issue keys carrying a role that reads the collection, say `reader`. Then, as one of your service's users, issue a key:
+
[source,bash]
----
curl -u alice@example.com:her-password -X POST https://<your-service>.restheart.com/keys   -H 'Content-Type: application/json' -d '{ "name": "claude", "roles": ["reader"] }'
----
+
Keep the `key` it returns: it is shown once. If your service has no users yet, create one on the link:/docs/cloud/managing-users[Users] page first. The full story is on the link:/docs/cloud/api-keys[API Keys] page.
. **Check what the agent will see.** Paste the key under **What an agent sees** and load. The collection you published is there, named by your service's URL. If it is not, the role cannot read it: fix the link:/docs/cloud/managing-permissions[permission], not the publication.
. **Connect Claude.** In Claude, **Settings → Connectors → Add custom connector**, paste the endpoint from **Connect an agent**, and when Claude asks you to sign in, sign in as that user — it uses the service's own sign-in page. Or, in a client that takes a header, send `Authorization: Bearer rhak_…`. Then ask: *"How many orders are still open?"*

The agent reads the catalogue, finds `orders`, reads it with the key's permissions, and answers. Everything it can do from here is what that user, with that role, can do over REST.

== Connecting an Agent

The **Connect an agent** panel gives you the endpoint and the four facts every client needs:

[cols="1,3"]
|===
| | Value

| Endpoint
| `https://<your-service>.restheart.com/mcp`

| Transport
| Streamable HTTP

| Configuration
| MCP 2025-06-18

| Headers
| `Authorization: Bearer <the agent's token>` and `Content-Type: application/json`
|===

The panel also has ready-made steps for the three common clients:

* **Claude** — Settings → Connectors, in the desktop app or on claude.ai. Add → Add custom connector, and paste the endpoint. Sign in when Claude asks: it runs the sign-in flow for you against your service's own login page.
* **Cursor** — add it from Cursor's own settings and paste the endpoint. The panel shows the `.cursor/mcp.json` it expects.
* **VS Code** — run "MCP: Add Server" from the Command Palette and paste the endpoint. The panel shows the `.vscode/mcp.json`.

For anything else — Zed, Windsurf, an agent you wrote yourself — the four facts above are all the configuration there is.

=== Give the Agent Its Own Credential

An agent reads with the permissions of whoever it authenticates as. The credential for an agent is an **API key** that a user of your service issues for it, carrying only the roles it needs: see link:/docs/cloud/api-keys[API Keys for Agents] for how to turn that on and how a key is issued.

Not your own account, and not the service administrator. An agent that authenticates as an administrator can read everything the administrator can.

=== The Sign-In Page

A client that signs the user in for you — Claude does — sends the user's browser to a sign-in page and brings them back afterwards. By default that is the page RESTHeart serves. Under **Sign-in page for agents** you can name your own instead: it receives the OAuth parameters as a query string and has to POST them back, with the user's credentials, to `https://<your-service>.restheart.com/authorize`.

The same panel lists **where a user may be sent back to**: the `redirect_uri` values `/authorize` accepts, `*` as a wildcard. It starts as the list every node accepts, which is what Claude and the desktop clients need. If you customise it, keep the `claude.ai` entry, or Claude can no longer connect: your list replaces the node's rather than extending it.

== Publishing Resources

The **Published resources** panel lists what an agent may use. It starts empty.

To publish something, click the **Add** button for its type — **Add collection**, **Add aggregation**, **Add change stream**, **Add GraphQL app** — and an inline form appears:

. **Which one** — pick from what is not published yet, of that type.
. **What it is for** — the description the agent reads to decide whether this is the resource it wants.
. Save.

The description is the part that matters. An agent picks a resource by reading it, so write what the thing holds or computes, not what it is called:

[cols="1,3"]
|===
| Type | What to write

| Collection
| What it holds, so an agent knows when to read it.

| Aggregation
| What it computes, and what its parameters mean.

| Change stream
| What changes it reports.

| GraphQL app
| What this API is for.
|===

To unpublish, remove the entry. The agent stops seeing it on its next catalogue read.

=== Parameters of a Parametric Aggregation

An aggregation whose pipeline uses `$var` takes parameters, and the page lists them under the entry, one row each, driven by the pipeline itself: add a variable to the stages and the row appears.

Give each one a type and a description. Left undescribed, an agent is told the parameter exists and nothing more, which in practice means it guesses.

== What an Agent Sees

The catalogue an agent gets depends on **who it signs in as**. The same service shows different things to different users, because permissions apply to MCP exactly as they apply to REST.

The **What an agent sees** panel lets you check that, with the credential the agent will actually use: paste an **API key** a user issued and read back the catalogue its agent gets. A **Password** works too, for a user who has not issued a key yet — with one caveat: a key carries only the roles it was issued with, so a password test shows what the *user* sees, not necessarily what their *key* does.

Two things the listing does not show on its own:

* The listing is only the first half. What an agent then *reads* is narrowed again by the permission's `readFilter` and `projectResponse`, which apply to MCP reads as they do to REST ones.
* A GraphQL app's schema is already per-caller here: a field its `@visible` directive hides from those roles is absent from what you see.

== Related Pages

* link:/docs/cloud/api-keys[API Keys for Agents] — the credential an agent uses, and how a user issues one
* link:/docs/cloud/managing-permissions[Managing Permissions (ACL)] — what an agent may read, including `readFilter` and `projectResponse`
* link:/docs/cloud/aggregations[Aggregation Pipelines] — where `$var` parameters come from
* link:/docs/cloud/graphql[GraphQL Applications] — and the `@visible` directive
