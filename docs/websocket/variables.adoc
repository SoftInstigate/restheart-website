---
docs_version: 9
title: Using Variables
layout: docs-adoc
menu: websocket
applies_to: both
redirect_from:
  - /docs/graphql-websocket/websocket/variables
  - /docs/mongodb-websocket/variables
---

The aggregation stages of the change stream can use _variables_, as in the following example:

[source,json]
----
{
    "uri": "mine",
    "stages": [
        { "$match": { "fullDocument::name": { "$var": "n" } } }
    ]
}
----

The `$match` stage uses the variable `n`, specified as `{ "$var": "n" }`.

Since 9.9.0, the client passes the variable as a plain query parameter named after it:

[source,http]
----
GET /messages/_streams/mine?n=Andrea HTTP/1.1
----

In case of the change stream *mine* described in the link:/docs/mongodb-websocket/examples[examples], the variable `n` is used to restrict notifications only to changes on documents with the property _name_ matching it.

A value is interpreted as JSON when it parses as one (so `?opts={"a":1}` binds `opts` to that object and `?tags=[1,2,3]` binds `tags` to that array), and otherwise taken literally as a string (so `?n=Andrea` binds `n` to `"Andrea"`). See link:/docs/mongodb-rest/aggregations#passing-variables[Passing Variables] for the full details — the mechanism is shared between aggregations and change streams.

==== Legacy: the `avars` Query Parameter

Before 9.9.0, all variables had to be bundled into a single JSON object passed via the `avars` query parameter:

[source,http]
----
GET /messages/_streams/mine?avars={"n":"Andrea"} HTTP/1.1
----

This form still works and remains fully supported. You can combine both forms in the same request; if a variable name is given both ways, the `avars` value wins.

=== Dot Notation support for `$var`

`$var` in RESTHeart change streams can utilize dot notation to access nested properties. This allows you to specify a complex variable structure and reference its components for more flexible filtering.

For example, if a client passes `?options={"name":"Andrea"}` (or, using the legacy form, `avars={"options":{"name":"Andrea"}}`), you can access the nested property `name` using dot notation within an aggregation stage, like this:

[source,json]
----
{
    "uri": "mine",
    "stages": [
        { "$match": { "fullDocument::name": { "$var": "options.name" } } }
    ]
}
----

The `$match` stage in the previous example uses the dot notation in `{ "$var": "options.name" }` to reference the nested property `name`, in this case `Andrea`.

By employing dot notation, you can work with structured and nested data effectively and make your change stream filtering more versatile and powerful.

=== Variables with Default Values

Variables can have a default value. This default value is used when the variable is not specified in the `?avar` query parameter.

For more detailed information on using variables with default values, please refer to the link:/docs/mongodb-rest/aggregations#variables-with-default-values[Aggregation documentation].

=== Predefined variables

The change stream aggregation can use _predefined variables_, as in the following example:

[source,json]
----
{
    "uri": "mine",
    "stages": [
        { "$match": { "fullDocument::owner": { "$var": "@user.sub" } } }
    ]
}
----

The `$match` stage uses the predefined variable `@user.sub`, specified as `{ "$var": "@user.sub" }`, and matches it against the document's `owner`.

NOTE: the predefined variables are resolved server side: the client does not pass them, and a value it supplies for one — `?avars={"@user.sub": "someone-else"}` — is ignored, so the stream stays scoped to whoever is connected.

IMPORTANT: use the name the caller's account actually carries. `@user.sub` identifies a caller authenticated with a JWT, `@user._id` one authenticated against MongoDB users, `@user.userid` one from the file realm — see link:/docs/security/permissions#identifying-the-caller[Identifying the caller]. A variable the account does not carry is unresolved, and since 9.8.2 an unresolved variable fails the request with `variable not bound` rather than binding `null`, which would have matched every document without that field.

Refer to link:/docs/mongodb-rest/aggregations#predefined-variables[Aggregation] documentation page for more information.

=== Security considerations

By default RESTHeart makes sure that the variables passed as query parameters don't contain MongoDB operators.

This behavior is required to protect data from undesirable malicious query injection.

Even though is highly discouraged, is possible to disable this check by editing the following property in the `restheart.yml` configuration file.

[source]
----
## Security

# Check if aggregation variables use operators.
# Allowing operators in aggregation variables is risky
# because requester can inject operators modifying the query

aggregation-check-operators: true
----
